// Reuse the same on-brand image for the Twitter card so twitter:image is set
// explicitly (it otherwise falls back to og:image, but this is unambiguous).
export { default, alt, size, contentType } from "./opengraph-image";
